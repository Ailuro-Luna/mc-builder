# Agent 建造操作指南

本文件只描述通用流程。若本机存在 `.local/server/AGENT_BUILDING_GUIDE.md`，先读取其中的部署入口、保护要求与现场记录；这些资料不得提交到公开仓库。实际任务以用户本次需求和实时状态为准。

## 开始前

1. 读取本地 `config.json`、服务器健康状态及已有任务状态。
2. 确认用户给出的维度、坐标含义、建筑范围和需要保留的设施。
3. 使用只读接口勘察目标及快照外扩区域，检查加载状态、方块实体和玩家/模组设施。
4. 有活动任务或未决批次时先核对，不能通过改写状态绕过保护。

MCP 提供状态、材料、方块、实体读取，以及蓝图预览、准备、施工、暂停、取消与撤销接口。实体包围盒不一定代表模组机械的实际占地。

## 蓝图与选址

蓝图放入本地 `plans/`。只提交虚构示例；真实场地蓝图默认被 Git 忽略。

```json
{
  "dimension": "minecraft:overworld",
  "operations": [
    {"from": [0, 64, 0], "to": [2, 66, 2], "block": "minecraft:stone", "hollow": true}
  ]
}
```

上例仅用于离线预览。`from`、`to` 包含端点；同一位置以最后一个操作为准；`hollow` 不会清空已有方块。目标不包含 NBT 或实体操作。

在用户授权的范围内，通过 `set_area.py --dimension ... --min ... --max ...` 配置两端策略。省略维度时默认为主世界。源水和既有方块替换分别需要蓝图与选址入口的显式开关，不得扩大区域或删除保护来让检查通过。

## 施工与验收

1. `preview_build` 编译蓝图，保存并返回任务 ID，不代表已勘察现场。
2. `prepare_build` 检查完整快照区域并保存原状；新环境或材料先在本次授权范围内试建、核验、撤销。
3. `start_build` 启动后台施工；返回 starting 不代表完成。
4. 通过 `build_status` 观察；遇到卸载、玩家改动、错误、未决批次或 epoch 改变时先核对。
5. 完成后独立读回全部目标，并检查入口、支撑、通行净空和实际用途。
6. 确认任务结束且无未决批次，执行 `set_area.py --disable`，保存世界，并把记录保存在本地。

普通暂停可按状态检查后继续施工；撤销暂停必须通过 `rollback_build` 继续。取消保留已放置的部分。已完成、取消或撤销的历史任务不可通过删记录强行重建。

只能通过 builder 公开接口施工和撤销，不得绕过它直接批量修改地图。快照与日志是恢复依据，收尾时不能删除。

## 信息管理

完整地址、端口、主机路径、玩家信息、坐标、保护列、任务 ID、日志及验收报告只留在本机。提交前执行 [公开内容检查](docs/PUBLICATION.md)，不得把完整现场工具输出复制到公开 PR 或 issue。
